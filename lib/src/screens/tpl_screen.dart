import 'package:flutter/material.dart';

class TplScreen extends StatelessWidget {

  Widget build(BuildContext context) {
    return new Container(
      margin: EdgeInsets.all(20.0),
      child: Column(
        children: [

        ]
      ),
    );
  }
}